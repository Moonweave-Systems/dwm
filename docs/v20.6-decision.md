# V20.6 Dogfood Replay Decision

Decision: keep

Command used to regenerate the V20.6 summary:

```bash
python scripts/dwm_dogfood_replay.py --manifest fixtures/v20.6/manifest.json --out out/dogfood-replay/v20.6-final
```

Generated summary values:

- `suite_id`: `v20.6-final`
- `fixture_count`: 4
- `required_fixture_count`: 4
- `required_passed`: 4
- `passed`: 4
- `failed`: 0
- `skipped`: 0
- `decision`: `keep`

The accepted replay requires repo status unchanged, `workflow-complete`, trusted
next output, and `recommendation.action` equal to `complete`.

This decision covers deterministic dogfood replay evidence only. It does not
claim live adapter execution.
